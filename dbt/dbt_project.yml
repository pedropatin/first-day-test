name: firstday
version: "1.0.0"
profile: firstday

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
analysis-paths: ["analyses"]

target-path: target
clean-targets: [target, dbt_packages]

models:
  firstday:
    staging:
      +materialized: table
    marts:
      +materialized: table
